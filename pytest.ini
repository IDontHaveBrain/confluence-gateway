[pytest]
log_cli = true
log_cli_level = ERROR
filterwarnings =
    ignore:::litellm.*
markers =
    unit: Unit tests (fast, no external dependencies)
    integration: Integration tests (requires external services)
    slow: Slow tests (timeout, heavy computation)
    api: Tests requiring Confluence API access
    semantic: Tests requiring vector DB and embeddings
